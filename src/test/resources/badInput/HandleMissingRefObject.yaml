openapi: 3.0.0
components:
  schemas:
    ContainsAllOf:
      type: object
      required:
        - propA
        - propB
      properties:
        propA:
          type: integer
        propB:
          $ref: "#/components/schemas/SomeAllOf"