openapi: 3.0.0
components:
  schemas:
    ContainsAllOf:
      x-fabric-resource-definition: true
      type: object
      required:
        - propA
        - propB
      properties:
        propA:
          type: integer
        propB:
          $ref: "#/components/schemas/SomeAllOf"