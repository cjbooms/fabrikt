openapi: 3.0.0
components:
  schemas:
    RequiredReadOnly:
      type: object
      required:
        - user_name
        - created
      properties:
        user_name:
          type: string
        created:
          type: string
          format: date-time
          readOnly: true