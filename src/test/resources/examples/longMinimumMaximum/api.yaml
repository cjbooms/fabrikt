openapi: "3.0.0"
info:
  title: "API Example"
  version: "1.0"
paths:
  /maximumTest/{pathId}:
    get:
      parameters:
        - in: path
          schema:
            type: integer
            format: int64
            minimum: 0
            maximum: 4294967295
          name: pathId
        - in: header
          name: headerid
          schema:
            type: integer
            format: int64
            minimum: 0
            maximum: 4294967295
        - in: query
          name: queryid
          schema:
            type: integer
            format: int64
            minimum: 0
            maximum: 4294967295
      responses:
        204:
          description: "result"
  /minimumTest/{pathId}:
    get:
      parameters:
        - in: path
          schema:
            type: integer
            format: int64
            minimum: -4294967295
          name: pathId
        - in: header
          name: headerid
          schema:
            type: integer
            format: int64
            minimum: -4294967295
        - in: query
          name: queryid
          schema:
            type: integer
            format: int64
            minimum: -4294967295
      responses:
        204:
          description: "result"
  /minMaxTest/{pathId}:
    get:
      parameters:
        - in: path
          schema:
            type: integer
            format: int64
            minimum: -4294967295
            maximum: 4294967296
          name: pathId
        - in: header
          name: headerid
          schema:
            type: integer
            format: int64
            minimum: -4294967295
            maximum: 4294967296
        - in: query
          name: queryid
          schema:
            type: integer
            format: int64
            minimum: -4294967295
            maximum: 4294967296
      responses:
        204:
          description: "result"
