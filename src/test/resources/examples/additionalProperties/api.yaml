openapi: 3.0.0
# See mapExamples for many more examples of how additional properties are used
components:
  schemas:
    Result:
      type: object
      required:
        - message
      properties:
        message:
          type: string
      additionalProperties: true
