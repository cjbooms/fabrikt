openapi: 3.0.0
components:
  schemas:
    OptionalVsRequired:
      type: object
      required:
      - name
      - requiredNullable
      properties:
        name:
          type: string
        gender:
          type: string
          format: uuid
        requiredNullable:
          type: string
          nullable: true