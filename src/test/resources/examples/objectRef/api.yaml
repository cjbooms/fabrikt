openapi: 3.0.0
components:
  schemas:
    ContainsObjectRef:
      type: object
      required:
        - weight_on_mars
      properties:
        weight_on_mars:
          $ref: "#/components/schemas/ObjectRef"
    ObjectRef:
      type: object
      required:
        grams
      properties:
        grams:
          type: integer
          example: 100