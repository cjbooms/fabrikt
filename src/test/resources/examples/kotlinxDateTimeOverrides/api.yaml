openapi: "3.0.0"
info:
  title: "DateTime Override Test"
  version: "1.0"
paths:
components:
  schemas:
    DateTimeModel:
      type: object
      properties:
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
